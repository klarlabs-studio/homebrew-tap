# Klarlabs Homebrew Tap

Homebrew formulas for Klarlabs tools.

```bash
brew tap klarlabs-studio/tap
brew install mnemos scout coverctl
```

Formulas are published automatically by each project's release pipeline.
